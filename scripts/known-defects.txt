# Known defects - the ledger. Currently EMPTY, and that is the point.
#
# Every line here names something broken right now with a test proving it. The run stays
# green while listed entries fail, so checks can land before fixes.
#
# It is SHRINK-ONLY, in both directions:
#   - a failure that is not listed here fails the run   (a regression)
#   - a line here that starts PASSING also fails the run (delete it to bank the fix)
#
# Thirteen entries were cleared at version 0.3.0. Nothing is outstanding.
#
# Format: <id> <one line saying what is wrong and who it hurts>
